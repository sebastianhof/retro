import {DashboardModel} from "../models/dashboardModel";
import {DashboardGraphModel} from "../models/dashboardModel";
import {DashboardItemModel} from "../models/dashboardModel";
import {DashboardShortcutModel} from "../models/dashboardModel";

export class DashboardActions {

    static addGraph(graph:DashboardGraphModel) {

    }

    static addItem(item:DashboardItemModel) {

    }

    static addShortcut(shortcut:DashboardShortcutModel) {

    }


    static setIndex(dashboard:DashboardModel, index:number) {

    }

}